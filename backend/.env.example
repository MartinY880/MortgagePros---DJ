# Spotify API Credentials
SPOTIFY_CLIENT_ID=your_spotify_client_id_here
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret_here
# For local development, HTTP is allowed for localhost
# For production, MUST use HTTPS: https://yourdomain.com/api/auth/callback
SPOTIFY_REDIRECT_URI=http://localhost:5000/api/auth/callback

# Server Configuration
PORT=5000
NODE_ENV=development

# Session Secret (generate a random string)
SESSION_SECRET=your_random_session_secret_here_change_this

# Frontend URL
FRONTEND_URL=http://localhost:5173

# Database
DATABASE_URL="file:./dev.db"
