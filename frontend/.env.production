# Production API endpoint (if backend is on different domain)
# Leave empty or comment out if backend is at same domain under /api
# VITE_API_URL=https://api.pros.mortgage/api

# Socket.io URL (usually same as API without /api suffix)
# VITE_SOCKET_URL=https://api.pros.mortgage
